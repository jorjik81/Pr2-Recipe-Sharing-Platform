<div class="text-center">
  <h2>{{name}}</h2>
  <p>🍕</p> <!-- Changed to pizza emoji -->
  <p>{{description}}</p>
  <p>Shared by {{user.name}} on {{format_date date_shared}}</p>
  <p>
    <span for="img" aria-label="food">🍕</span> <!-- Changed to pizza emoji -->
    <span class="food-quantity">{{food_quantity}} available</span>
  </p>
  <p>
    <button class="btn btn-primary">Support This Initiative!</button>
  </p>
</div>
